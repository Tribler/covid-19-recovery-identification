<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" package="com.immune"
    tools:ignore="ImpliedTouchscreenHardware,MissingLeanbackSupport,
    MissingApplicationIcon,MissingTvBanner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>-->

    <application
        android:allowBackup="false"
        android:theme="@android:style/Theme.NoDisplay">
        <receiver android:name=".CertServiceStarter">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.immune.android.START_SERVICE" />
                <action android:name="com.immune.android.STOP_SERVICE" />
            </intent-filter>
        </receiver>
        <service
            android:name=".CertService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:isolatedProcess="false" />
        <!--Just for debugging. Remove this activity later.-->
        <activity android:name=".CertActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <!--Just for debugging. Remove this activity later.-->
    </application>

</manifest>
